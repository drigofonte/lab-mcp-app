{
  "name": "lab-mcp-app",
  "private": true,
  "scripts": {
    "dev": "concurrently -n backend,frontend,sandbox -c blue,green,yellow \"pnpm --filter backend dev\" \"pnpm --filter frontend dev\" \"pnpm --filter sandbox dev\"",
    "sandbox:dev": "pnpm --filter sandbox dev",
    "build": "pnpm -r build"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "@nestjs/core"
    ]
  }
}